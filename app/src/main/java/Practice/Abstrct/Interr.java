package Practice.Abstrct;
abstract class Interr { // We write abstract keyword before the class name

    public abstract void add(); //If we create any simple method in abstract class then we will have to add abstract keyword before of that simple method name
    
    public void abc(){// We can also create method body in abstract class but this method should not be with abstract
        
    }
    int a = 120;
}
